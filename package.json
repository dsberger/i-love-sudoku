{
  "name": "i-love-sudoku",
  "version": "1.0.0",
  "description": "A live Sudoku solver in raw JS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "node-sass --output-style compressed src/styles/main.scss dist/styles.css",
    "watch:css": "npm run build:css -- -w src/styles"
  },
  "author": "Dan Berger <dsberger@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "3.8.0"
  }
}
